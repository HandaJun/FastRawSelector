<Window x:Class="FastRawSelector.VIEW.RawCopyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FastRawSelector.VIEW"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="18"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"       
        Background="{DynamicResource MaterialDesignPaper}"
        mc:Ignorable="d"
        Title="{DynamicResource Loc.RawCopyTitle}" Height="375" Width="900"
        MinHeight="375" MaxHeight="375"
        MinWidth="600" WindowStyle="None"
        WindowStartupLocation="Manual" Loaded="Window_Loaded" Topmost="False"
        Closing="Window_Closing" AllowsTransparency="True">
    <Border BorderBrush="{DynamicResource AppChromeBorderBrush}" BorderThickness="1" Background="{DynamicResource MaterialDesignPaper}" Margin="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Background="#01000000" MouseDown="TitleGrid_MouseDown">
                <Image Source="/FastRawSelector;component/FastRawSelector.ico" 
                       VerticalAlignment="Center" HorizontalAlignment="Left" Margin="13"/>
                <TextBlock Text="{DynamicResource Loc.RawCopyTitle}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="47,0" />
                <Button Click="CloseBt_Click"
                        Style="{StaticResource MaterialDesignFlatButton}" HorizontalAlignment="Right" Height="50"
                        Foreground="{DynamicResource AppTitleIconBrush}">
                    <Button.Content>
                        <iconPacks:PackIconBoxIcons Kind="RegularX"
                                        Width="18" Height="18" Foreground="{DynamicResource AppTitleIconBrush}"
                                        VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Button.Content>
                </Button>
            </Grid>

            <Grid Grid.Row="1">
                <Grid Margin="10,20,10,30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="80"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{DynamicResource Loc.CopyTarget}" Style="{StaticResource TitleStyle}"/>
                    <ComboBox x:Name="CopyTargetCb" Grid.Column="1" SelectedIndex="0" 
                      Style="{StaticResource MaterialDesignComboBox}"
                      SelectionChanged="CopyTargetCb_SelectionChanged">
                        <ComboBoxItem x:Name="SelectedCbi" Content="{DynamicResource Loc.CopySelected}" Tag="Selected"/>
                        <ComboBoxItem x:Name="SpecifiedFolderCbi" Content="{DynamicResource Loc.CopySpecified}" Tag="SpecifiedFolder"/>
                    </ComboBox>

                    <TextBlock Text="{DynamicResource Loc.CopyRawFolder}" Style="{StaticResource TitleStyle}" Grid.Row="1"/>
                    <TextBox x:Name="RawPathTb" Height="30" Grid.Column="1" Grid.Row="1" Margin="0,0,70,0" 
                     TextChanged="TextBox_TextChanged"/>
                    <Button x:Name="RawPathOpenBt" Content="{DynamicResource Loc.Open}" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Right"
                    Click="RawPathOpenBt_Click" Style="{StaticResource MaterialDesignRaisedButton}"/>

                    <TextBlock Text="{DynamicResource Loc.CopyJpegFolder}" Style="{StaticResource TitleStyle}" Grid.Row="2"/>
                    <TextBox x:Name="JpegPathTb" Height="30" Grid.Column="1" Grid.Row="2" Margin="0,0,70,0"
                     IsEnabled="False" TextChanged="TextBox_TextChanged"/>
                    <Button x:Name="JpegPathOpenBt" Content="{DynamicResource Loc.Open}" Grid.Column="1" Grid.Row="2" HorizontalAlignment="Right"
                    Click="JpegPathOpenBt_Click" Style="{StaticResource MaterialDesignRaisedButton}"
                    IsEnabled="False" />


                    <iconPacks:PackIconBoxIcons Kind="SolidDownArrow" Grid.Row="3" Grid.ColumnSpan="2"
                                        Width="25" Height="25" Foreground="{DynamicResource AppAccentBrush}"
                                        VerticalAlignment="Center" HorizontalAlignment="Center"/>

                    <TextBlock Text="{DynamicResource Loc.CopyDest}" Style="{StaticResource TitleStyle}" Grid.Row="4"/>
                    <TextBox x:Name="CopyPathTb" Height="30" Grid.Column="1" Grid.Row="4" Margin="0,0,70,0"
                      TextChanged="TextBox_TextChanged"/>
                    <Button Content="{DynamicResource Loc.Open}" Grid.Column="1" Grid.Row="4" HorizontalAlignment="Right"
                Click="CopyPathOpenBt_Click" Style="{StaticResource MaterialDesignRaisedButton}"/>

                    <Grid Grid.Row="5" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                        <Button Content="{DynamicResource Loc.CopyRun}" Width="200" Height="60" Click="CopyBt_Click" 
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        FontSize="20"/>
                    </Grid>
                </Grid>
                <ProgressBar x:Name="CopyPb" VerticalAlignment="Bottom" Height="10" Value="0" Maximum="100" Visibility="Hidden"/>
            </Grid>
        </Grid>
    </Border>
</Window>